import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
    root: {
      width: '100%',
    },
    container: {
      maxHeight: 440,
      marginTop: 10
    },
    icon:{
      margin: 5
    }
  });